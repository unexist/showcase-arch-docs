configuration:
    title: Test
    pkgs:
        - "github.com"

rules:
    -   name_regexp: ".*"
        pkg_regexps:
            - ".*/adapter"
        component:
            description: "adapter component"
            tags:
                - ADAPTER
    -   name_regexp: ".*"
        pkg_regexps:
            - ".*/domain"
        component:
            description: "domain component"
            tags:
                - DOMAIN
    -   name_regexp: ".*"
        pkg_regexps:
            - ".*/infrastructure"
        component:
            description: "infrastructure component"
            tags:
                - INFRASTRUCTURE